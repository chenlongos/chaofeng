# 周报 — 2026.08.03 ~ 2026.08.07

## 本周工作

### 1. SG2002 UART1/2 回环失败 — 时钟根因定位与修复 (08-06)

**问题**：UART1 (/dev/ttyS1) 和 UART2 (/dev/ttyS2) 回环测试，TX 发送 35 字节成功，RX 收到 0 字节。短接 A18/A19 已确认。

**根因**：
- CLKGEN 的 `CLK_EN_1` 寄存器（offset 0x004）中 UART1/2 的 core clock 和 APB clock 未显式使能
- RSTC 的 `SOFT_RSTN_0`（offset 0x000）中 UART1/2 复位位未释放

**修复**：
- 在 `api/src/vfs/dev/tty_serial.rs` 中添加 CLKGEN_BASE / RSTC_BASE 常量
- 添加 `enable_uart1_clock_and_reset()` 和 `enable_uart2_clock_and_reset()`
- 在 `new_tty_s1()` 和 `new_tty_s2()` 开头调用
- 编译通过（Rust nightly-2026-01-01, riscv64gc），生成 boot.sd (1.55MB)

### 2. cpumask panic 修复 (08-06 ~ 08-07)

**问题**：板子启动后立即 panic：
```
panicked at cpumask-0.1.0/src/lib.rs:124:9:
assertion failed: index < SIZE
```

**根因**：
- `axplat-riscv64-sg2002` crate 的 `init.rs` 中 `axplat::percpu::init_primary(0)` 曾被注释掉
- 导致 per-CPU 区域未初始化，`this_cpu_id()` 返回垃圾值，触发 `cpumask::CpuMask::one_shot()` 硬断言

**修复**：取消注释 `init_primary(0)` 调用，重新编译。

**版本管理问题**：
- 修复版编译时间 12:37（boot.sd.v2, MD5: `b6909a12...`）
- SD 卡上实际部署的是旧版 11:48（MD5: `f45b0c8c...`），不含 percpu 修复
- 修复版已准备好，下次上板时替换部署

### 3. SD 卡文件整理 (08-07)

- 清点 SD 卡 FAT32 分区所有文件（boot.sd 多个历史版本、fip.bin、YOLO 模型等）
- 备份当前 boot.sd → boot.sd.bak_20260807
- 编写部署检查清单（MD5 校验、sync 命令）

## 下周计划

- [ ] 板子上部署 boot.sd.v2，验证 cpumask panic 已修复
- [ ] 短接 A18/A19 跑 UART1/2 回环测试，确认时钟修复生效
- [ ] 回环通过后接回 ESP32 验证电机通信
- [ ] 继续检测管线集成调试
